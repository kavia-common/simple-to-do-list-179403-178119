{
  "name": "react-kavia",
  "version": "0.2.0",
  "private": true,
  "dependencies": {
    "idb-keyval": "^6.2.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "sql.js": "^1.13.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "prestart": "node ./scripts/copy-wasm.js || true",
    "build": "react-scripts build",
    "prebuild": "node ./scripts/copy-wasm.js || true",
    "test": "react-scripts test --watchAll=false",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
